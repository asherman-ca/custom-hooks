import Container from "@/app/components/Container";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <Container>
      <h1>List Title</h1>
    </Container>
  );
};

export default page;
